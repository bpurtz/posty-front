const styles = (theme) => ({
  pagination_wrap: {
    width: '100%',
    display: 'grid',
    justifyContent: 'center',
    gridTemplateColumns: '3em 3em',
    gridColumnGap: '1em',
    padding: '1em',
    boxSizing: 'border-box'
  }
})

export default styles
